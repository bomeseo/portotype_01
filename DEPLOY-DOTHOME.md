# 불티 기능 전환본 — 닷홈 연결 안내

**9월 8일 변경분과 준비된 관리자/DB 설정 파일은 [최신 DB 안내](DB-UPDATE-0908.md)를 먼저 보세요.** 아래 관리자 CLI 생성 방식은 초기 관리자 SQL을 대신하는 선택지이며 두 방식을 중복 사용하지 않습니다.

기존 색상·글꼴·레이아웃을 유지했습니다. 데모 안내, 예시 데이터 로딩, 가짜 답장과 체험 인증을 운영 화면에서 제거했습니다. 관리자와 로그인 화면은 기존 컴포넌트 스타일을 사용합니다.

## 구현된 기능

- 이메일/비밀번호 가입·로그인·로그아웃, 비밀번호 해시 저장, 서버 세션, CSRF 검사, 요청 횟수 제한
- 전화번호 인증번호 발송·확인·만료·시도 제한, 인증번호 1회 사용, 전화번호 로그인. 문자 발송 업체 연결 전에는 실패 사유를 표시합니다.
- 상품 사진 업로드·이미지 재인코딩, 등록·수정·삭제와 소유권 검사
- 서버 가격 검증·입찰·낙찰·거래 생성. 데이터베이스 잠금으로 동시에 들어온 요청을 순서대로 처리합니다.
- 상품별 회원 간 채팅, 서버 저장, 안 읽은 메시지, 재접속 복원, 중복 전송 방지, 계좌 전달, 개인 차단
- 계정별 찜, 구매·판매 내역, 당사자별 거래 단계, 완료 거래의 중복 방지 후기
- 문의 접수와 운영자 메일 발송 시도, 문의/신고 답변, 관리자 회원 제한·해제 및 상품 중지, 관리자 처리 기록
- 관심 카테고리, 선호지역·거래 방식, 거래 내역 공개 여부, 실제 최근 활동 표시 및 공개 설정
- 한국 표준시 기준 방해금지 시간과 상대방 안내, 방해금지 중 화면 내 새 메시지 안내 억제

## 실제 연결·운영 확인이 필요한 부분

1. 닷홈 상품명·도메인·PHP 버전·DB 이름·HTTPS 및 접속 방법 확인. 이 작업에서는 실제 닷홈 DB에 접속하거나 배포하지 않았습니다.
2. 문자 발송 업체 계약·발신번호 등록·API 연결. 현재 설정은 미연결이며 실제 문자를 보내거나 인증 성공을 흉내 내지 않습니다.
3. 운영진 수신 메일과 발신 메일. PHP mail() 지원 여부와 실제 받은편지함 도착을 확인해야 합니다. 미지원이면 업체 API/SMTP 발송기를 연결해야 합니다.
4. 관리자 두 명의 이메일과 각각의 비밀번호로 아래 명령을 두 번 실행해야 합니다. 공용 관리자 비밀번호나 기본 계정은 없습니다.
5. 1분마다 경매 마감을 실행하는 예약 작업. 접속 요청 때도 마감이 처리되지만, 무접속 중 마감 시점에 처리하려면 예약 실행이 필요합니다.
6. 외부 전화번호·계좌 사기조회는 아직 연결하지 않았습니다. 현재는 관리자 판단에 따른 계정 정지와 인증된 전화번호의 재사용 제한입니다. 이메일 가입만으로 동일인의 재가입을 완전히 막지는 못합니다.
7. 브라우저를 닫았을 때의 푸시, 실제 문자 알림, 결제·에스크로, 이메일 소유권 인증·비밀번호 찾기는 이번 구현에 포함하지 않았습니다.

공개 운영 완료 상태가 아니라 서버 연결과 소규모 검증을 위한 구현본입니다. 개인정보 처리방침·이용약관·운영 연락처, 백업·복원과 실제 호스팅 부하를 공개 전에 확인해야 합니다.

## 설치

필요 환경: PHP 8.0 이상, PDO MySQL, GD, 세션, MySQL/InnoDB, HTTPS, 사진 저장 경로 쓰기 권한. Apache에서는 .htaccess의 접근 제한이 적용되어야 합니다.

1. `npm run build`로 `dist`를 생성하고 **dist 내용만** 닷홈 웹 루트에 올립니다. 프로젝트 전체·Git·테스트·이전 빌드 아카이브를 올리지 않습니다. 기존 uploads는 배포 때 지우지 않습니다.
2. `server/config.example.php`를 참고해 **웹 루트 바깥**에 `bullty-config.php`를 만듭니다. 예: `/계정/html`이 웹 루트라면 `/계정/bullty-config.php`. 다른 경로는 서버 환경변수 `BULLTY_CONFIG`로 지정합니다. 실제 자격증명은 저장소에 넣지 않습니다.
3. phpMyAdmin에서 `server/schema.sql`을 가져오거나 SSH에서 `php server/manage.php install`을 실행합니다. 기존 테이블을 삭제하지 않는 초기 설치입니다.
4. `uploads`를 PHP가 쓸 수 있도록 설정합니다. 서버 코드와 업로드 폴더의 .htaccess도 포함합니다.
5. HTTPS 주소로 접속해 가입·로그인 후 서로 다른 두 계정으로 상품과 채팅을 확인합니다.

### 관리자 두 명 만들기

SSH의 해당 프로세스 환경에 `BULLTY_ADMIN_PASSWORD`를 설정한 뒤 다음 명령을 각각 실행합니다. 비밀번호는 명령 인자나 Git에 넣지 않습니다. 12자 이상이며 두 계정은 다른 비밀번호를 사용합니다.

```text
php server/manage.php admin 실제이메일 운영자이름
```

명령 실행 후 프로세스 환경의 비밀번호 값을 제거합니다. 두 명이 이미 있으면 추가 생성은 거절됩니다. 관리자 계정으로 로그인하면 내 정보에 ‘운영 관리’가 나타납니다.

### 문자와 메일

비공개 설정의 `sms_sender`에 `function(string $phone, string $code): bool` 형태의 서버 함수를 연결합니다. 실제 발송 업체가 접수를 승인한 경우에만 true를 반환해야 합니다. 인증번호를 로그나 웹 응답으로 노출하면 안 됩니다.

메일 설정은 `support_email`, `mail_from`입니다. 문의는 먼저 DB에 저장됩니다. mailStatus의 accepted는 발송 시스템 접수이며 받은편지함 도착 확인은 아닙니다. failed도 문의가 삭제되지 않고 운영 관리에서 확인 가능합니다.

### 경매 예약 마감

호스팅에서 지원하는 예약 작업으로 `php /절대웹루트/server/manage.php settle`을 1분마다 실행합니다. 예약 실행이 지원되지 않는 상품에서는 별도 예약 실행 수단을 결정해야 합니다. 임의로 공개 마감용 URL을 노출하지 않습니다.

## 검증과 구조상 한계

`npm test`: 화면·서버 응답 처리 검사. PHP가 있으면 HTTP 통합 검사와 서버 거래 규칙 검사까지 실행합니다. PHP 경로는 `BULLTY_PHP`로 지정할 수 있습니다. 통합 검사는 임시 SQLite DB와 테스트 전용 문자 발송기로 실행하며 운영 DB·실제 문자·메일을 사용하지 않습니다.

`npm run dev`: PHP 개발 서버입니다. 비공개 DB 설정이 필요합니다. 단순 파일 열기나 정적 호스팅에서는 서버 기능이 작동하지 않습니다.

현재 서버는 초기 소규모 서비스용으로 레코드를 컬렉션별 JSON 행에 저장하고 전역 트랜잭션 잠금을 사용합니다. 채팅 화면은 약 3초, 다른 화면은 약 15초마다 동기화하고 숨긴 탭은 쉬게 합니다. 전체 상태를 가져오는 구조여서 사용자·상품·대화가 늘기 전에 페이지 단위 조회와 변경 메시지만 받는 방식, 테이블별 인덱스 및 잠금 범위를 개선해야 합니다. 닷홈 MySQL에서 동시 접속 부하·메일·예약 실행까지 검증한 것은 아닙니다.

기존 `tests/domain.test.cjs`, `tests/screens.test.cjs`는 이전 프로토타입 동작의 참고 기록이며 현재 npm test에서는 실행하지 않습니다. 예시 데이터 파일도 운영 빌드에는 포함하지 않습니다.
