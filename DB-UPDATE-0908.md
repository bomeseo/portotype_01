# 9월 8일 DB 기능 구현

디자인은 유지했습니다. agents.md의 ‘나중에’ 항목인 AI 봇·제휴 링크·앱 내 결제는 추가하지 않았습니다.

## 이번 변경

- `bullty_members` 회원 전용 테이블: 회원 아이디, 이메일, 전화번호, 비밀번호 해시, 닉네임, 권한, 이용 상태, 평균 평점, 후기 수. 관심사·선호지역·거래 방식·공개 범위·방해금지 시간은 회원 프로필에 저장합니다.
- 아이디 또는 이메일 로그인, 아이디 중복 방지, 비밀번호 변경과 다른 기기 세션 해제.
- 평점은 실제 완료 거래 후기를 기준으로 계산해 DB에 저장합니다. 회원은 평점이나 관리자 권한을 스스로 바꿀 수 없습니다.
- 관리자 전화번호/계좌 사기 확정, 영구 이용 제한, 처리 기록. 사기 확정 제재는 일반 해제 버튼으로 풀 수 없습니다.
- 채팅의 ‘계좌 전달’은 은행·계좌·예금주를 구조화해 저장합니다. 운영진이 해당 계좌의 사기를 확인하면 계좌 재전달을 차단합니다. 자유 입력 메시지 속 계좌를 모두 자동 탐지하거나 외부 사기정보를 조회하는 기능은 아닙니다.
- 기존 상품·입찰·채팅·찜·후기·신고·문의·거래 내역은 서버 DB 저장을 유지했습니다.

## 주신 DB 정보와 관리자 준비 파일

주신 비밀번호로 프로젝트 바깥 `../bullty-private/bullty-config.php`에 비공개 DB 설정을 준비했습니다. DB 사용자와 DB 이름은 `bullty`, 호스트는 **닷홈에 올라간 PHP 기준** `localhost`로 설정했습니다. 닷홈 관리 화면의 DB 이름이 다르면 설정을 맞춰야 합니다. 이 컴퓨터의 localhost에 닷홈 DB가 있다는 뜻은 아닙니다.

`../bullty-private/admin-bootstrap.sql`에는 `bullty_admin1`, `bullty_admin2` 초기 등록문이 있습니다. 비밀번호는 각각 다르게 생성해 같은 폴더의 `admin-credentials.txt`에 보관했고 SQL에는 해시만 넣었습니다. 이 파일들은 공개 웹 폴더에 올리지 않습니다.

**아직 실제 닷홈 DB에 계정이나 테이블을 생성한 것은 아닙니다.** 다음 순서로 연결합니다.

1. 최신 `dist` 내용만 웹 루트에 업로드합니다. 기존 업로드 이미지는 지우지 않습니다.
2. 비공개 설정 파일을 실제 서버의 웹 루트 바로 위에 `bullty-config.php` 이름으로 배치합니다.
3. phpMyAdmin에서 `server/schema.sql`을 가져옵니다.
4. 같은 DB에 비공개 초기 관리자 SQL을 가져옵니다. 기존 계정을 덮어쓰지 않으며 관리자 두 명이 이미 있으면 추가하지 않습니다.
5. 초기 관리자 비밀번호로 로그인한 뒤 비밀번호를 변경합니다. 변경해야 운영 관리 화면을 사용할 수 있습니다.

이전 공용 레코드에 회원 데이터가 이미 있다면 SSH의 `php server/manage.php install`로 새 회원 테이블에 옮깁니다. 상품·대화·후기는 삭제하지 않습니다. SSH를 사용할 수 없다면 기존 회원 데이터 유무를 확인한 뒤 별도 이관 절차를 진행하세요.

DB 계정으로 FTP 업로드가 가능하다고 가정하지 않았습니다. 실제 업로드에는 정확한 사이트 주소와 FTP/SSH 연결 정보가 필요합니다. DB 비밀번호를 다시 제공할 필요는 없습니다.

## 외부 연결과 검증

문자 발송 업체와 운영진 메일은 아직 미연결입니다. DB가 있다고 실제 문자 발송이나 외부 사기정보 조회까지 자동으로 제공되지는 않습니다. 외부 연동은 미연결 상태를 분명히 표시합니다. 전화번호 사기 확정은 인증된 번호에만 가능합니다.

오늘 Windows Application Control이 PHP 실행을 차단했습니다. 화면/요청 처리·PHP 문법 분석·관리자 해시 생성은 검사했지만 변경된 PHP의 실제 서버·MySQL 실행 검증은 완료하지 못했습니다. 통합 테스트는 저장했으며 PHP 실행이 허용된 환경에서 `BULLTY_PHP`를 지정해 실행해야 합니다. 이전 9월 7일 테스트 통과가 이번 변경의 실행 검증을 대신하지 않습니다.

회원은 전용 테이블에 저장하지만 다른 기능은 컬렉션별 JSON 행과 전역 트랜잭션 잠금을 사용하는 초기 구조입니다. 공개 전 실제 닷홈에서 부하·백업·메일·예약 마감을 검증하고, 이용 규모에 따라 페이지 조회와 변경 메시지 조회 방식으로 개선해야 합니다.
