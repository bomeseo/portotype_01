# 불티 운영 연결 검증 — 2026-09-09 최신 상태

**DB 연결은 해결됐습니다. 이전 연결 오류를 처음부터 재조사하지 않습니다.**

사이트: https://bullty.dothome.co.kr/
저장소: https://github.com/bomeseo/portotype_01
확인한 원격 main: `233a849`

## 연결 복구 이력

- 이전 점검에서 비어 있던 DB에 `bullty_members`, `bullty_records`, `bullty_lock`을 생성했습니다.
- 팀원 전달에 따르면 파일질라에서 사이트 파일 경로는 `/html`, 비공개 설정 파일은 `/bullty-config.php`입니다.
- 팀원이 누락된 설정 파일을 생성·이동한 뒤 DB 인증 오류 1045를 해결했습니다. 실제 변경한 설정 항목은 전달받지 않았습니다.
- 이번 점검에서도 `/api/index.php`의 HTTP 200, `ok: true`를 직접 확인했습니다.
- FTP 호스트·계정·포트·접속 방식은 아직 전달받지 않았습니다. DB 로그인 정보와 같다고 가정하지 않습니다.

## 실제 닷홈에서 검증한 기능

별도 테스트 계정 두 개와 명확히 테스트라고 표시한 상품으로 검사했습니다. 실제 입찰이나 금전 거래는 하지 않았습니다.

- 두 계정의 회원가입, 로그아웃 후 아이디·비밀번호 로그인
- 사진 업로드, 상품 등록, 다른 계정에서 상품 조회
- 다른 계정의 상품 삭제 요청 거부
- 구매자 → 판매자 메시지, 판매자 → 구매자 답장
- 읽지 않은 메시지 수, 읽음 처리, 중복 전송 방지
- 재로그인 후 대화 복원, 비로그인 방문자에게 대화 비공개
- 테스트 상품 삭제 처리 후 공개 목록에서 제외
- 브라우저 화면에서도 로그인, 채팅방 표시, 메시지 전송, 로그아웃 확인

최종 API 검증 보고서: `../bullty-private/live-smoke-mtu5xa6i.json`
회원가입 최초 검증 기록: `../bullty-private/live-smoke-mtu5w224.json`

최초 실행은 테스트 이미지 자료 문제로 중단됐고, 다음 실행은 호스팅의 HTML 오류 응답을 검사 도구가 처리하지 못해 중단됐습니다. 올바른 이미지와 응답 처리로 재검사한 최종 실행은 통과했습니다. 애플리케이션 코드를 수정한 것은 아닙니다.

테스트 계정 두 개, 삭제 상태인 테스트 상품 두 개, 테스트 사진·대화 기록은 DB/서버에 남아 있습니다. 테스트 계정 식별 정보는 비공개 검증 기록에 보관합니다. 공개 상품 목록은 검증 종료 시 0개입니다. 기존 데이터나 사진은 삭제하지 않았습니다.

테스트 계정 비밀번호는 `../bullty-private/live-smoke-mtu5w224-credentials.json`에만 보관합니다. 이 파일과 `bullty-private` 폴더는 Git이나 공개 웹 폴더에 업로드하지 않습니다.

## 팀원이 파일질라에서 처리할 항목

1. **`/html/bullty-check-7c92.php` 삭제**: 이번 확인에서 HTTP 200으로 아직 공개돼 있었습니다. 삭제 요청은 전달했으며 완료 여부는 미확인입니다. 삭제 뒤 URL이 더 이상 진단 내용을 반환하지 않는지 확인합니다.
2. **개발용 파일 정리**: 아래 파일들이 실제로 공개 응답했습니다.
   - `/html/tests/runtime.test.cjs`
   - `/html/scripts/prepare-dothome.cjs`
   - `/html/.openai/hosting.json`
   - `/html/package.json`
3. 정리 전 서버 파일을 백업하고, 위 개발용 폴더·파일은 `/html` 밖으로 이동합니다. 이후에는 빌드된 `dist`의 내용만 `/html`에 배포합니다. `dist`를 덮어쓴다고 기존 개발용 파일이 자동 제거되지는 않습니다.
4. **`/bullty-config.php`, `/html/uploads`의 기존 사진, DB 데이터는 보존합니다.** 서버 전체 폴더 삭제·DB 초기화를 하지 않습니다. API와 화면에서 사용하는 파일도 임의로 삭제하지 않습니다.

이번 확인에서 `/agents.md`는 404, `/server/schema.sql`은 403이었습니다. 모든 경로를 검사한 것은 아닙니다.

## 운영 설정 — 완료 여부 확인 필요

- 관리자 두 계정: 생성·초기 비밀번호 변경 여부를 팀원에게 확인해야 합니다. 이번 검증에서는 관리자 권한을 만들거나 변경하지 않았습니다.
- 문자 발송: 공급자 연결, 발신번호 설정, 실제 수신 검증 여부를 확인해야 합니다. 임의의 전화번호로 문자를 발송하지 않았습니다.
- 문의 메일: `support_email`, `mail_from` 설정과 실제 수신 여부를 확인해야 합니다. 테스트 메일은 보내지 않았습니다.
- 경매 예약 마감: 호스팅의 예약 실행 등록 여부를 확인해야 합니다. API 요청 시 마감 처리와 무접속 상태에서의 예약 실행은 별도입니다.

추가 발견: 거부된 API 요청에서 서버의 JSON 대신 호스팅의 HTML 오류 페이지가 반환되는 경우가 있습니다. 정상 기능은 통과했지만 잘못된 입력의 구체적인 안내 문구는 별도 보완·검증 대상입니다.

## 다음 진행

팀원이 진단 파일 삭제와 운영 설정 완료 항목을 알려주면 확인합니다. 서버 파일 수정이 필요할 때는 실제 FTP 접속 방법을 확보한 뒤 진행합니다. 이번 검증에서는 애플리케이션 코드 수정·서버 파일 재배포를 하지 않았습니다. 이 메모를 GitHub에 반영하는 것은 닷홈 서버 배포와 별개입니다.
