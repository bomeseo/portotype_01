{
  "name": "live-auction-prototype",
  "version": "0.3.0",
  "private": true,
  "description": "A modular Korean auction marketplace prototype",
  "scripts": {
    "dev": "node scripts/serve.cjs",
    "build": "node scripts/build.cjs",
    "test": "node --test tests/*.test.cjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "linkedom": "0.18.12",
    "prettier": "3.6.2"
  }
}
